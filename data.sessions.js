/* Session library that ships with the site.
   Regenerate this file from the planner: Export > Publish library file,
   drop it back into js/, redeploy. Library sessions are read-only and
   visible on every device that opens the site. */
window.LIBRARY = { published: '', sessions: {} };
